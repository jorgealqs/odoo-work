<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <!-- model.name action client -->
    <record id="action_lottery_baloto_manual" model="ir.actions.client">
        <field name="name">Manual</field>
        <field name="tag">action.LotteryBalotoManual</field>
        <field name="target">current</field>
    </record>

    <!-- This Menu Item must have a parent and an action -->
    <menuitem
        id="action_manual_menu_act"
        name="Manual"
        parent="lottery_forecast.lottery_menu_root"
        action="action_lottery_baloto_manual"
        sequence="100"
    />

</odoo>