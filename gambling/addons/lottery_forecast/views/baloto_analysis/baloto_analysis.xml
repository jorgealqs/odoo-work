<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <!-- model.name action client -->
    <record id="action_lottery_baloto_analysis" model="ir.actions.client">
        <field name="name">Analisys</field>
        <field name="tag">action.BalotoForecast</field>
        <field name="target">current</field>
    </record>

    <!-- This Menu Item must have a parent and an action -->
    <menuitem
        id="action_manual_analysis_menu_act"
        name="Analisys"
        parent="lottery_forecast.lottery_baloto_fourth_level_menu"
        action="action_lottery_baloto_analysis"
        sequence="100"
    />

</odoo>