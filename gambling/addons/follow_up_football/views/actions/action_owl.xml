<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <!-- model.name action client -->
    <record id="action_test_model_name" model="ir.actions.client">
        <field name="name">Test</field>
        <field name="tag">config.testAction</field>
        <field name="target">current</field>
    </record>

    <!-- This Menu Item must have a parent and an action -->
    <menuitem
        id="action_test_menu_act"
        name="Match"
        parent="follow_up_football.football_country_second_level_menu"
        action="action_test_model_name"
        sequence="1"
    />

</odoo>