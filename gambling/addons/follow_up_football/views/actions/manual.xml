<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <!-- model.name action client -->
    <record id="action_manual_name" model="ir.actions.client">
        <field name="name">Manual</field>
        <field name="tag">action.ApiFootballUserManual</field>
        <field name="target">current</field>
    </record>

    <!-- This Menu Item must have a parent and an action -->
    <menuitem
        id="action_manual_menu_act"
        name="Manual"
        parent="follow_up_football.football_country_second_level_menu"
        action="action_manual_name"
        sequence="100"
    />

</odoo>