<?xml version='1.0' encoding='utf-8'?>
<odoo>

    <record id="view_football_api_wizard" model="ir.ui.view">
        <field name="name">football.api.wizard Form</field>
        <field name="model">football.api.wizard</field>
        <field name="arch" type="xml">
            <form string="Sessions">
                <sheet>
                    <div class="oe_title">
                        <h1 class="text-center mb-4">
                            <span class="fa fa-globe"></span> Sync Football League Sessions
                        </h1>
                    </div>
                    <div class="alert alert-info" role="alert">
                        Download sessions by league data from the Football-API. Click "Sync" to start the process.
                    </div>
                    <footer>
                        <button string="Sync" type="object" name="action_sync_sessions" class="btn btn-success" icon="fa-download"/>
                        <button string="Cancel" class="btn btn-secondary" special="cancel"/>
                    </footer>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_football_api_wizard" model="ir.actions.act_window">
        <field name="name">League Sessions</field>
        <field name="res_model">football.api.wizard</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="view_football_api_wizard"/>
        <field name="target">new</field>
    </record>

</odoo>