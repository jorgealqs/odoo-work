<?xml version='1.0' encoding='utf-8'?>
<odoo>

    <record id="view_football_league_wizard" model="ir.ui.view">
        <field name="name">football.league.wizard Form</field>
        <field name="model">football.league.wizard</field>
        <field name="arch" type="xml">
            <form string="Leagues">
                <sheet>
                    <div class="oe_title">
                        <h1 class="text-center mb-4">
                            <span class="fa fa-globe"></span> Sync  Leagues
                        </h1>
                    </div>
                    <div class="alert alert-info" role="alert">
                        Download leagues data from the Football-API. Click "Sync" to start the process.
                    </div>
                    <footer>
                        <button string="Sync" type="object" name="action_sync_leagues" class="btn btn-success" icon="fa-download"/>
                        <button string="Cancel" class="btn btn-secondary" special="cancel"/>
                    </footer>
                </sheet>
            </form>
        </field>
    </record>


    <record id="action_football_league_wizard" model="ir.actions.act_window">
        <field name="name">Leagues</field>
        <field name="res_model">football.league.wizard</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="view_football_league_wizard"/>
        <field name="target">new</field>
    </record>

</odoo>